---
name: software-certificate-skill
description: 面向普通用户，从真实软件项目全自动生成中国软件著作权申请资料：一次收集登记事实，自动分析业务、选择可追溯源码、通过 Chrome DevTools、Computer Use 或用户截图取得界面证据，生成申请表信息、规范黑白灰操作手册、代码前后30页或全部材料、真实 DOCX/PDF、质量报告、哈希、备份与回滚。适配 Codex、Claude Code、Cursor、OpenCode、WorkBuddy、QoderWork、TraeWork 及支持 Agent Skills 或 AGENTS.md 的平台。用户提到软件著作权、软著申请资料、申请表、代码材料、操作手册或软著审查时使用。
---

# 软件著作权申请资料生成器

## 面向用户的默认体验

用户只需说“为当前项目生成软件著作权申请资料”。先用通俗语言说明：

- 将读取并分析当前真实项目，不套用虚构业务；
- 将集中收集一次无法可靠推断的登记事实；
- 将让用户一次选择截图方式；
- 后续项目分析、业务理解、源码选择、截图、写作、排版、转换、检查、修复和打包由 Agent 完成；
- `申请表信息.txt` 用于复制到登记系统；正式 PDF 按登记系统当日要求上传；DOCX 用于 Word/WPS 复核；质量报告只用于内部留档。

不要先展示脚本、路径参数、JSON、置信度或内部阶段。用户明确要求高级细节时再展开。

正式资料至少生成：

```text
软件著作权申请资料/
  正式资料/
    申请表信息.txt
    <软件全称>_操作手册.docx
    <软件全称>_操作手册.pdf
    <软件全称>-代码(前30页).docx/.pdf  # 源码达到60页
    <软件全称>-代码(后30页).docx/.pdf  # 源码达到60页
    <软件全称>-代码(全部).docx/.pdf    # 源码不足60页
    生成报告.md
    提交材料清单.md
  质量检查/
    材料一致性校验报告.json/.md
    代码来源追溯清单.json
    截图清单.json
    待确认事项清单.md
    SHA256SUMS.txt
```

只向普通用户显示这些进度：正在检查运行环境、正在分析项目、正在理解软件业务、正在整理申请表、正在选择真实源码、正在获取页面截图、正在生成操作手册、正在生成代码材料、正在渲染并检查文档、正在自动修复问题、已完成可以检查并提交。

## 执行入口

把本文件所在目录解析为 `SKILL_ROOT`。输出始终写入目标项目，不写入 Skill 安装目录。

1. 内部执行 `python SKILL_ROOT/scripts/product_workflow.py prepare --project PROJECT_ROOT`。
2. 将生成的“一次性基础信息表”作为一张集中表单呈现；优先复用会话、项目或历史材料中的已确认事实。
3. Agent 自己完成 `.工作区/business-understanding.json`，不得让普通用户填写技术或业务推断表。
4. 展示一张截图方式选择卡；已有明确选择时直接采用。
5. 信息齐全后内部执行 `python SKILL_ROOT/scripts/product_workflow.py resume --project PROJECT_ROOT`。
6. 依据工作流状态继续未完成阶段；不要重复扫描、重复询问或创建重复目录。

在有 bundled Python/文档运行时的平台优先使用该运行时。PDF 必须由 Word、WPS 或 LibreOffice 真实转换并逐页渲染；不得用空文件或改扩展名代替。

## 一次性登记事实

只集中确认：软件全称、简称、版本、著作权人类型/名称/证件、完成日期、开发方式、原创或修改、发表状态及条件日期、权利取得方式、权利范围、必要权属说明、截图方式。

自动推断：软件分类、用途、用户、行业、核心价值、主要功能、技术特点、语言、开发与运行环境、源程序量、操作流程、手册模块和源码范围。

内部保存每个推断的建议值、证据、置信度、理由、是否影响登记事实和是否需确认。冲突优先级：已确认申请事实 > 正式配置 > 真实运行结果 > 其他项目资料。只有名称、版本、权属、日期、发表状态或多个独立申请范围发生实质冲突时，才集中询问一次。

## 业务证据

阅读 README、产品说明、配置、路由、页面、组件、控制器、服务、API、模型、状态管理、测试、部署、既有截图和真实运行结果。文件名与关键词只作阅读线索，不能直接成为业务结论。

每项关键功能至少绑定页面、路由、代码、测试、文档、截图或已确认事实之一。正式手册不得出现项目中不存在的菜单、按钮、角色、数据、接口、提示或结果。

详细模型见 [references/evidence-model.md](references/evidence-model.md) 与 [references/product-workflow.md](references/product-workflow.md)。

## 截图方式

- **Chrome DevTools**：推荐给 Web 系统；检查工具、启动/连接项目、等待就绪、执行登录/点击/输入/滚动，在标题、控件、URL或文本断言通过后截图。记录 URL、角色、动作、时间、尺寸、SHA-256、控制台与失败请求；自动重试并保留诊断图。
- **Computer Use**：推荐给桌面端、Electron、模拟器和复杂交互。先读取当前应用状态，再操作和截图；每张图绑定章节、功能和结果。若平台只返回会话画面而没有可保存文件，集中提示一次并切换“用户自行截图”。
- **用户自行截图**：生成简短任务清单；导入 `用户截图/` 后自动排序、去重、清晰度检查、统一命名和章节匹配。
- **暂时跳过**：生成带可见截图预留位置的文字版，并把缺图写入报告；补图后只重建手册和相关报告。

Web 自动化细节见 [references/screenshot-automation.md](references/screenshot-automation.md)。

## 源码与代码材料

默认自动选择真实应用入口、路由、页面、核心组件、业务服务、API封装、状态、模型、算法、工具和必要后端逻辑。自动排除依赖、构建物、压缩/生成代码、锁文件、测试夹具、示例、重复/无关模块、敏感信息及来源不清代码。

使用完整源文件顺序，保留路径、原始行号、文件哈希和显示换行映射。每逻辑页至少 50 行，显式分页。达到 60 页时分别生成连续前30页与后30页；不足60页时生成全部。

DOCX 转 PDF 后核对实际页数、显示行密度、长行、空白/溢出/截断、页眉名称版本、前后卷连续性和 DOCX/PDF对应关系。失败时自动调整字号、宽度、行距、分页或长行策略并重试。详见 [references/source-selection.md](references/source-selection.md)。

## 操作手册

默认生成审核人员和普通使用者均能理解的操作手册。页数由真实功能、操作闭环、截图和必要说明自然决定；不设置40、60或66页目标，不以重复截图、定义、优势或空泛段落凑页。

每个功能章节写清用途、入口、可见内容、操作、限制、成功反馈、异常反馈和真实截图。正文使用用户语言，不把框架、接口封装、状态管理或数据库实现当成操作说明。

统一采用 A4 正常软著黑白灰版式：宋体正文、黑体标题、浅灰表头、克制页眉页脚、真实 Word TOC 域与可点击目录。图片保持比例，不拉伸、不裁掉关键控件。版式见 [references/visual-system.md](references/visual-system.md) 与 [references/layout-benchmarks.md](references/layout-benchmarks.md)。

## 单一事实源、验证与修复

所有材料从 `.工作区/application-facts.json` 派生。名称、简称、版本、权利人、日期、发表状态、开发方式、权利取得、权利范围、功能、源码量、材料页数、标题、页眉页脚、文件名和截图标题必须一致。

发布前检查：文件完整性、DOCX结构、真实PDF和逐页渲染、字段长度、事实一致、源码抽样回溯、第三方与敏感信息、页数连续性、业务证据、截图质量和章节匹配、空白/截断/溢出、占位符、哈希与发布包。一般问题自动局部修复；只把登记事实、权属或真实业务范围冲突放入待确认清单。

工作流保存机器状态和阶段哈希；重复运行幂等。名称/版本变化只重建依赖材料，补图只重建截图清单、手册和报告。覆盖正式文件前备份；用户手工修改的 DOCX 先归档。`product_workflow.py rollback` 恢复最近快照。

质量门禁见 [references/quality-gates.md](references/quality-gates.md)。

## 规则与平台

启动时显示规则快照日期。读取 [references/rules-2026.md](references/rules-2026.md)；规则过旧只提示刷新，不阻塞草稿。区分国家版权局规章、中国版权保护中心/当前系统、政府高校行业办理说明、公开案例经验；保存发布日期、抓取日期、适用范围和等级。经验做法不得表述为统一法定要求。

安装及平台能力见 [references/agent-platforms.md](references/agent-platforms.md)。同一证据、事实和生成流程适配 Codex、Claude Code、Cursor、OpenCode、WorkBuddy、QoderWork、TraeWork；仅工具调用层不同，不复制多套业务规则。

## 最终回复

只列出：正式资料绝对路径、每个文件用途、要上传的PDF、要复制到官网的TXT、必须人工确认的问题、简明提交步骤。高级证据、命令和JSON仅在用户要求时展开。
